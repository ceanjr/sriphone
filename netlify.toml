[build]
  publish = "dist"
  command = "npm run build"

# Ignorar a verificação de secrets
[build.environment]
  SECRETS_SCAN_ENABLED = "false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
